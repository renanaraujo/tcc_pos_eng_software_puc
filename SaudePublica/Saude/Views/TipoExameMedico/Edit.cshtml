@model Saude.Models.Entity.Tipo_Exame_Medico

@{
	ViewData["Title"] = "Editar tipo de exame médico";
}

<h1>Editar tipo de exame médico</h1>

<hr />

<form asp-action="Edit">
	<div class="row">
		<div class="col-md-12">
			<div asp-validation-summary="All" class="text-danger"></div>
		</div>
	</div>
	<div class="row">
		<div class="col-md-4">
			<input type="hidden" asp-for="Id" />
			<div class="form-group">
				<label aria-label="Nome" asp-for="Nome" class="control-label"></label>
				<input aria-label="Nome" asp-for="Nome" class="form-control" />
				<span asp-validation-for="Nome" class="text-danger"></span>
			</div>
			<div class="form-group">
				<label aria-label="Descricao" asp-for="Descricao" class="control-label"></label>
				<textarea aria-label="Descricao" asp-for="Descricao" class="form-control"></textarea>
				<span asp-validation-for="Descricao" class="text-danger"></span>
			</div>
			<div class="form-group">
				<input type="submit" aria-label="Salvar" value="Salvar" class="btn btn-primary" />
			</div>
		</div>
	</div>
</form>

<div>
	<a class="btn btn-info" aria-label="Voltar para a lista" asp-action="Index">Voltar para a lista</a>
</div>

@section Scripts {
	@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
